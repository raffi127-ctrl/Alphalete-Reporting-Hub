# Real access codes and locally-stored structures never belong in the public repo.
.codes.json
data/

# ICD payout reference is generated by the puller from the real org DD pull —
# do not commit the partial local-sample version.
icd_payout.json
