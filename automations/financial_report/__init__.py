"""Financial report — fills the financial section of the focus-report sheets
from the emailed FINANCIAL SUMMARY workbooks. Its own report: the user uploads
the workbooks, then runs it; it parses them and writes the financial rows
across the focus-report Google Sheets."""
