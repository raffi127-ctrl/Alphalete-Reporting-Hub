playwright>=1.45
playwright-stealth>=2.0
openpyxl>=3.1
gspread>=6.1
google-auth-oauthlib>=1.2
keyring>=25.0
streamlit>=1.36
# Used by uploaded automations that need a stealth Chrome (Cloudflare /
# ownerville login). patchright is a Playwright fork with anti-detection
# patches. Added 2026-05-22 when JD's Order Log report imported it.
patchright>=1.60
# Needed by Order Log + future uploaded reports producing .xlsx files
# in the requester's Downloads folder.
pandas>=2.0
python-dateutil>=2.8
# Frontier OPT Data Pull parses the emailed Frontier PDFs. Added 2026-05-24;
# without it Eve's Windows Hub crashes on the Frontier run (ModuleNotFoundError).
pdfplumber>=0.11
slack_sdk
certifi
