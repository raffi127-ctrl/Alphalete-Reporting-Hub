"""Digi Docs + Onboarding Quizzes — the third new-start onboarding step.

Sends each eligible new start their OwnerVille document bundle and ticks the
"Digi Docs" column; a separate read-only sweep ticks "Onboarding Quizzes" once
the rep has finished the six training courses themselves.

Sibling of blueink_docs (Blue Ink packet) and headshots (Headshot Photo). Same
weekly cohort on the same `D2D OBCL <m.d>` tab, same Slack room, one column
each. All three report onto the merged Hub card, shared/new_start_steps.py.

The click-path this implements is written up, with screenshots-worth of detail
and its open questions, in workflows/digi-docs-onboarding-quizzes.md. Read that
before changing anything here.
"""
