streamlit>=1.30
pandas>=2.0
gspread>=5.10
google-auth>=2.20
# rebuild 2026-07-25: force a full Cloud reboot so the process re-imports
# gross_profit fresh (stale sys.modules cache dropped ProductPlan.tier_bonuses).
# rebuild 2026-07-26: icd_payout gained payouts_for_office() — full reboot so the
# cached module reloads (a stale one would AttributeError on the new function).
# rebuild 2026-07-26b: box/base energy mappers + combined table — reboot to reload.
