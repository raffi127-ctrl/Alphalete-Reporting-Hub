Drop the real swag-package photo here as:  swag-card.png  (or .jpg)

The New-Hire Swag Texts tool (automations/swag_welcome/) writes each new
hire's name onto this photo's card. Until the real photo is here, the tool
uses a labeled placeholder so previews still render.

After adding the photo, tune where the name sits in:
  automations/swag_welcome/compose.py  →  NAME_BOX
